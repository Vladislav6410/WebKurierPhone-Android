<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Network -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Voice / WebRTC -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!-- Camera for OCR / document translation -->
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- Notifications (calls, mission prompts, alerts) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Optional: keep CPU running during calls (can be refined later) -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:allowBackup="false"
        android:label="WebKurierPhone"
        android:supportsRtl="true">

        <activity
            android:name="com.webkurier.android.MainActivity"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

    </application>
</manifest>