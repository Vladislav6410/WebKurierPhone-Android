# Android placement

VERIFIED repo roots: `docs/` and `app/src/main/assets/`.

Documentation path: `docs/education/`.
Runtime lesson path: `app/src/main/assets/education/lessons/week01/lesson01/`.

The runtime path is suitable for packaged static lesson assets. Parsing/loading UI code is a separate implementation task.
